# AI Expense Tracker — Final

Features:
- Dual Theme (Light / Dark) across all pages
- Permanent full left sidebar
- Top tabs (Add / Recent / Stats)
- Category dropdown + chips + icons
- Analytics (pie + line charts) — solid color palette
- Predictions page (naive demo)
- History page with delete + export CSV
- ML training script to build `model/expense_model.pkl`